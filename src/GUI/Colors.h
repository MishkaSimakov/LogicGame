#ifndef COLORS_H
#define COLORS_H

#include "SFML/Graphics.hpp"

namespace gui
{
    namespace colors
    {
        const sf::Color black(0, 18, 25);
        const sf::Color gray(218, 222, 223);
        const sf::Color light(233, 216, 166);
        const sf::Color white(255, 255, 255);


        const sf::Color primary(10, 147, 150);
        const sf::Color dark_primary(0, 95, 115);

        const sf::Color warning(238, 155, 0);
        const sf::Color dark_warning(202, 103, 2);

        const sf::Color danger(174, 32, 18);
        const sf::Color dark_danger(155, 34, 38);
    }
}

#endif // RELATIVEBUTTON_H
